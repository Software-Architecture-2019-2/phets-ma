import { StyleSheet, Dimensions } from 'react-native';

export const ProfileStyles = StyleSheet.create({
    ProfileImage: {
        width: 150,
        height: 150,
        borderRadius: 150 / 2
    },
});